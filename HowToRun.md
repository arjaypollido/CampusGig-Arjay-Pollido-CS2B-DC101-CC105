Database Setup: Run source contact_db.sql via MySQL to prepare the environment.

Connectivity: Define your local SQL credentials within the db constant in backend/server.js.

Deployment: In your terminal use cd backend followed by node server.js to initialize the API.

Entry Point: Access the application frontend via index.html.