Database Setup: Run source schema.sql via MySQL to prepare the environment.

Connectivity: Define your local SQL credentials within the db constant in server.js.

Deployment: Use cd backend followed by node server.js to initialize the API.

Entry Point: Access the application frontend via index.html.
